# Copy to .env to override defaults. Both settings are optional.

# Where the accounts store (with credentials) lives. Gitignored.
ACCOUNTS_FILE=accounts.json

# How many messages to fetch per account.
DEFAULT_FETCH_LIMIT=15
